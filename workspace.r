#Workspace

#Print the current work directory - cwd
getwd() 
#list the objects in the current directory
ls()

#change directory
setwd(mydirectory)
setwd("c:/docs/mydir")
setwd("/usr/alladio/mydir")

#display last 25 commands
history()